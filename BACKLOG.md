# Backlog

This file stores ideas and non-urgent tasks so they do not live only in chat memory.

## Near Term

- **→ [Session agenda 2026-06-18](session-agenda-2026-06-18.md)** — casting audit, brand content DB/rules, QC follow-ups.
- Build 5-10 test videos per day from short scripts.
- Keep testing `1 script -> 3 visual styles`.
- Add a rule/check for screen recording preview frames before render.
- Make CTA secondary text brighter and more stable.
- Create a simple metrics table or UI for manual stats entry.
- Keep collecting hooks and rank them after real results.

## Content And Assets

- Add more European-looking women with clear emotions.
- Add couple conflict / relationship tension scenes.
- Add money stress / planning / luxury planning scenes.
- Add more product-safe screen recordings.
- Explore Pexels photos for CTA, product cards, and abstract inserts.
- Generate more CTA backgrounds if current five become repetitive.

## Automation

- Make render scripts read screen recording rules automatically.
- Add DB fields/tags for screen recording safe zones.
- Add a draft QC helper that extracts:
  - hook frame;
  - product frames;
  - CTA frames;
  - contact sheet.
- Add validation that CTA backgrounds use `safe_zone` and `button_zone`.
- Add validation that fresh Pexels were audited before render.

## Metrics And Learning

- Build a local stats entry UI.
- Track per-video:
  - hook;
  - script;
  - visual style;
  - CTA;
  - assets used;
  - views;
  - 3-second hold;
  - average watch time;
  - clicks/leads.
- Learn which styles work per angle.
- Separate hook performance from visual style performance.

## Future Product Idea

The system could become a creator/arbitrage tool:

- user uploads own assets;
- system indexes assets;
- Pexels/API generation fills gaps;
- multiple scripts/styles render automatically;
- metrics reveal which hooks, scenes, CTAs, and angles work;
- trend and competitor analysis feeds the creative loop.

This is future-facing. Current priority is making the local workflow reliable.

